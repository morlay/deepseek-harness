- 不使用 emoji，不使用 em dash。
- 标题简短，**Title Case** 格式。
- 绝不混用等宽字体和粗体标记——是关键词用粗体，是代码/路径用反引号。
- 避免嵌套列表。不写"如上所述"、"见下文"这类方向性引用——每段自包含。
- 真实本地文件用 Markdown 链接：`[filename](/abs/path/to/file:line)`，不用 `file://` 等 URI。
